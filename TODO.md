# 待办事项

本文件只保留当前尚未完成的工作。已经完成的实施过程、历史方案和旧测试数字不重复放在这里，分别见 `CHANGELOG.md` 与 `docs/superpowers/`。

## P0：阻塞本地可靠使用

- [ ] 使用一个明确的 Worker 状态目录重新注册/启动 Native Worker，确认平台健康从 `degraded` 恢复为 `ok`。
- [ ] 在保持 Windows 唤醒且只运行一个 Worker 的条件下，重新完成 24 小时监控采样；要求覆盖率至少 95%、最大间隔不超过 90 秒、未解释失败为 0，并记录日志大小、journal 大小和待补报数量变化。
- [ ] 解释最近一次采样在第 480 个样本提前失败的原因，避免只重跑而不分析失败证据。

## P1：正式本地版本的核心验收

- [ ] 在专用 Windows 工作区完成 junction/reparse point、文件占用、恢复冲突和并发目录变化验收；确认工作区外哨兵文件不变。
- [ ] 在独立 PostgreSQL 数据库和备份目录完成一次生成备份、预检、停机恢复和恢复后动作/审计检查。
- [ ] 使用外部客户端完成真实的受限 token 撤销、过期、跨工作区、重复提交、审批拒绝和结果查询验证。
- [ ] 为监控目标增加经过认证和审计的禁用/归档/删除接口，再清理历史失效登记；在此之前不要直接修改数据库。
- [ ] 检查首次使用、空状态、加载失败、权限拒绝、Worker 阻塞和恢复冲突页面，补齐缺失的中文提示和测试。

## P2：交付和维护能力

- [ ] 在干净 Windows 环境按 README 从安装到真实文件动作走通，并记录依赖、端口、迁移和恢复证据。
- [ ] 实际运行 GitHub Actions 的 API/Web、PostgreSQL 和 Windows Worker job；把跳过项和环境原因写入发布证据。
- [ ] 将依赖审计加入 CI，并检查锁文件在 API、Worker 和 Docker 构建中的一致性。
- [ ] 完成上一版本升级、失败退出和备份恢复回归，确认启动失败不会被旧服务健康状态掩盖。
- [ ] 增加 Markdown 内部链接检查和文档命令冒烟检查，避免 README 与脚本再次漂移。

## P3：可选增强

- [ ] 增加本地通知或可配置告警出口，但必须保持回环默认和不发送敏感内容。
- [ ] 在现有只读边界内增加更多 Windows 服务/本机 HTTP 连接器；不扩展为全盘扫描、任意命令执行或内核拦截。
- [ ] 在核心可靠性验收通过后，再评估多用户、角色、远程部署和集中式密钥管理。
